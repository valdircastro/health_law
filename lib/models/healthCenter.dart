import 'package:world_time_app/models/medicament.dart';

class HealthCenter{
  String name;
  String adress;
  String coord;
  int distance;

  HealthCenter(this.name, this.adress, this.coord, this.distance);
}